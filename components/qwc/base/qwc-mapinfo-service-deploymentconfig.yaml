apiVersion: apps.openshift.io/v1
kind: DeploymentConfig
metadata:
  labels:
    io.kompose.service: qwc-mapinfo-service
  name: qwc-mapinfo-service
spec:
  replicas: 1
  selector:
    io.kompose.service: qwc-mapinfo-service
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        io.kompose.service: qwc-mapinfo-service
    spec:
      serviceAccountName: qwc-sa
      containers:
        - env:
            - name: JWT_COOKIE_CSRF_PROTECT
              value: "True"
            - name: JWT_COOKIE_SAMESITE
              value: Strict
            - name: JWT_SECRET_KEY
              value: my-secret-key
            - name: SERVICE_MOUNTPOINT
              value: /api/v1/mapinfo
          image: ' '
          name: qwc-mapinfo-service
          volumeMounts:
            - mountPath: /srv/pg_service.conf
              name: qwc-mapinfo-service-cm0
              readOnly: true
              subPath: pg_service.conf
            - mountPath: /srv/qwc_service/config
              name: qwc-config-pvc
      restartPolicy: Always
      volumes:
        - configMap:
            items:
              - key: pg_service.conf
                path: pg_service.conf
            name: qwc-mapinfo-service-cm0
          name: qwc-mapinfo-service-cm0
        - name: qwc-config-pvc
          persistentVolumeClaim:
            claimName: qwc-config-pvc
  test: false
  triggers:
    - type: ConfigChange
    - imageChangeParams:
        automatic: true
        containerNames:
          - qwc-mapinfo-service
        from:
          kind: ImageStreamTag
          name: qwc-mapinfo-service:latest-2025-lts
      type: ImageChange
